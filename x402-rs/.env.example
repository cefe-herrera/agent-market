# Wallet that signs x402 settlements on BSC. Must hold BNB/tBNB for gas.
FACILITATOR_PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Used at Docker build time (Coolify Build Argument): mainnet | testnet
NETWORK=mainnet

# Runtime (optional — defaults shown)
RUST_LOG=info
HOST=0.0.0.0
PORT=8080
